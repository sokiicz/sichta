# sichta

> Status: wip — created 2026-09-20

## Setup
```bash
pnpm install
pnpm dev
```

## Env vars
See `D:/ai/Apps/Secrets/sichta.txt` (local only, not committed).

## Deploy
GitHub Pages — set `base: '/sichta/'` in `vite.config.ts`, push, enable Pages on `gh-pages` branch.
